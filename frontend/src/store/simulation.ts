// The store now lives in core/store.ts (the single source of truth). This module is kept
// as a compatibility re-export so existing import paths continue to resolve.
export { store } from "../core/store";
